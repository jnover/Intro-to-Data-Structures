Name: John Nover

This project allows a user to do a very basic encoding of a file using Huffman's encoding algorithm. There are two such binary files produced with the  "make" command, encode and decode.
Encode allows the user to encode a file, and then Decode decodes that file back to the original state. 
